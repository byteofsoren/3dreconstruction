Camera module
*************
The camera module handles the input camera stored in ../camera directory.
It handles camera calibration from aruco corners, storing and loading the results
from the directory.

.. automodule:: camera
   :members:
   :undoc-members:
   :show-inheritance:
