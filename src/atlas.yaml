---
# Configuration and setting for the map ojbect.
origin_aruco: 0
view_obj:
  use_camera_matrix: True
transfer_obj:
  weight_calculation_method: "simple"
sort_aruco: False
save:
  showmap: False
  saveimg: True
  mapimg: '../results/atlas.png'

# Selecting method for calulating the epipolar geometry.
# 0 Select a camera and the nearest camera.
# 1 Select a camera and every camera
#   containing that feature.
epipolar_geometry_method: 0
