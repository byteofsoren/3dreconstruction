---
# Read set configurations not sepecific to each set.
log:
  file : "../logs/readset.log"
  # format : "%(name)s - %(levelname)s - %(message)s"
  format : "%(name)s:%(levelname)s:%(lineno)s-> %(message)s"
  level: 10

sets:
  path: "../datasets/{name}"
  setfile: "set.yaml"

